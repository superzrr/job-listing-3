<h1>职缺详情:<%= @job.title %></h1>
<p>
  <%= simple_format(@job.description) %>
</p>

<table class="table">
<% @job.resumes.each do |f| %>
<tr>
  <td>简历</td>
  <td><%= f.content %></td>
</tr>
<tr>
  <td>下载</td>
  <td><%= f.attachment_url %></td>
</tr>
<% end %>
</table>
